/**
 * ИГРОВАЯ КОМНАТА — список мини-игр (владелец правит сам).
 *
 * ══ ЗАЧЕМ ОТДЕЛЬНАЯ КОМНАТА, А НЕ СРАЗУ ИГРА ══
 *
 * Тут были обе крайности, и обе забракованы.
 *
 * Сначала за кнопкой «Настроение» открывался маленький список прямо
 * на главном экране. Тестировщик-подросток его не заметил: нажал и ждал,
 * что игра развернётся, «а потом увидел маленькую плашечку под этим всем».
 *
 * Тогда игру стали запускать сразу, первым нажатием — и это забраковал
 * владелец 27.07: **«я сказал, там будет ещё несколько игр. Нужно как-то
 * иначе делать. Подготовить для них место и комнату выбора».**
 *
 * Отсюда нынешнее устройство: «Настроение» открывает КОМНАТУ во весь
 * экран. Игра там пока одна, но комната сразу выглядит как комната —
 * с местом под то, что появится дальше.
 *
 * ══ КАК ДОБАВИТЬ ИГРУ ══
 *
 * 1. Написать саму игру отдельным компонентом (образец — `mini-game.tsx`,
 *    игра 2048: 8-битная, без картинок, правила вынесены в `lib/game-2048.ts`
 *    и проверяются отдельно от экрана).
 * 2. Добавить строчку в `MINI_GAMES` ниже.
 * 3. Показать её в `components/game-room.tsx` — там один `switch` по id.
 *
 * Порядок в списке = порядок в комнате.
 */

export type MiniGameEntry = {
  id: string;
  title: string;
  subtitle: string;
  /** Значок — текстом: игры у нас 8-битные, картинок не заводим. */
  badge: string;
};

export const MINI_GAMES: MiniGameEntry[] = [
  {
    id: "2048",
    title: "2048",
    subtitle: "Складывай плитки, пока есть ходы",
    badge: "2048",
  },
];

/**
 * Сколько пустых мест показать под будущие игры.
 *
 * Это не заглушка ради красоты. Комната из одной карточки читается как
 * «тут всё» — а нам надо, чтобы читалось «тут будет больше». Пустые места
 * честно говорят: игра пока одна, но комната растёт.
 *
 * Игр станет много — поставить 0, места сами исчезнут.
 */
export const GAME_ROOM_PLACEHOLDERS = 2;

/** Что написано наверху комнаты. */
export const GAME_ROOM_TITLE = "Во что поиграем";

export const GAME_ROOM_NOTE =
  "Поиграешь — призрак повеселеет. Голду за это не дают: это отдых, а не работа.";

export const GAME_ROOM_SOON = "Скоро";
