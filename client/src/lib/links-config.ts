/**
 * Ссылки наружу (владелец правит сам).
 * - kind: "telegram" — открывается внутри Телеграма (openTelegramLink),
 *   "web" — во внешнем браузере (openLink). Для t.me всегда ставь "telegram".
 * - порядок в массиве = порядок на экране.
 */

export type OutboundLink = {
  id: string;
  title: string;
  subtitle: string;
  url: string;
  kind: "telegram" | "web";
  emoji: string;
};

/**
 * ГДЕ У НАС РАЗГОВАРИВАЮТ. Одна дверь на всё приложение.
 *
 * ══ ОТДЕЛЬНОГО ЧАТА У ШКОЛЫ НЕТ. Это важно и это решено ══
 *
 * Владелец 27.07: «у нас везде ссылка идёт не на чат, а именно на группу
 * в тг. Там хоть и есть возможность комментить посты, но это непонятно
 * будет куда скидывать». На прямой вопрос ответил: **чата нет, работы
 * кидают в КОММЕНТАРИИ ПОД ПОСТАМИ канала.**
 *
 * Значит поломка была не в адресе, а в СЛОВАХ. Призрак говорил «скинь
 * работу в чат», ребёнок нажимал, попадал в ленту новостей и не понимал,
 * куда, собственно, кидать. Адрес всё это время был правильный — врало
 * обещание. Поэтому по всему приложению «чат школы» заменён на честное
 * «комментарии под постом» и «наш Телеграм».
 *
 * ══ ЗАЧЕМ ТОГДА ОТДЕЛЬНАЯ ЗАПИСЬ, А НЕ ПРОСТО СОЦСЕТЬ ══
 *
 * У этих двух ссылок разная работа, даже когда адрес совпадает:
 * - эта — РАЗГОВОР. Сюда ведут все «покажи работу», «спроси, если
 *   застрял», «там голосуют». Все такие двери берут адрес отсюда через
 *   `COMMUNITY_LINK` в `community-config.ts`;
 * - `SOCIAL_LINKS` ниже — витрина: канал, YouTube, ВК. Читать и смотреть.
 *
 * Заведётся когда-нибудь настоящий чат — поменять надо будет ОДНУ строку
 * `url` здесь, и все двери переедут разом.
 *
 * ══ ПОЧЕМУ АДРЕС С НОМЕРОМ НА КОНЦЕ, А НЕ ПРОСТО КАНАЛ ══
 *
 * `/363` — это номер КОНКРЕТНОГО поста. Владелец 28.07 прислал его сам:
 * это его закреплённый пост «Я Сергей, преподаю 3D школьникам вживую»,
 * он висит в закрепе постоянно, и комментарии под ним открыты.
 *
 * Разница для ребёнка большая. Ссылка на канал целиком высаживала его
 * в ленту: он прочитал «покажи работу в комментах» — и должен сам искать,
 * под каким постом это уместно. Ссылка на пост открывает сразу нужное
 * место, и кнопка «Прокомментировать» у него прямо под носом.
 *
 * ⚠️ Номер поста — часть адреса, а не украшение. Сотрёшь его — вернётся
 * старое «иди поищи». Поменяется закреплённый пост — сюда надо принести
 * НОВЫЙ номер (в Телеграме: нажать на пост → «Копировать ссылку»).
 */
export const SCHOOL_CHAT: OutboundLink = {
  id: "school-chat",
  title: "Наш Телеграм",
  subtitle: "Комментарии под закреплённым постом: показать работу и спросить",
  url: "https://t.me/jcenterskids/363",
  kind: "telegram",
  emoji: "💬",
};

/**
 * ПРИЁМКА ДОМАШЕК — отдельная группа под ежедневные скриншоты.
 *
 * ══ ЗАЧЕМ ОНА ПОЯВИЛАСЬ ══
 *
 * Решение владельца 28.07, и оно про две РАЗНЫЕ вещи, которые до этого
 * шли в одну дверь:
 *
 *   промежуточный шаг дня  → сюда, в приёмку: отдельная группа, куда
 *                            входят по приглашению. Их много, они сырые,
 *                            и это нормально: пять в неделю с каждого;
 *   ГОТОВЫЙ проект недели  → в комментарии под закреплённым постом
 *                            (`SCHOOL_CHAT` выше). Там витрина школы,
 *                            туда идут только красивые финальные работы.
 *
 * Его слова: «итоговые варианты проекта — потому что это же наш закреп,
 * пусть там будут красивые работы. А все промежуточные этапы… если в него
 * лить, это будет спам какой-то лютый».
 *
 * ⚠️ НЕ СВОДИТЬ ЭТИ ДВА АДРЕСА В ОДИН. Если промежуточные скрины пойдут
 * под закреплённый пост, витрина превратится в ленту черновиков, и туда
 * перестанет быть не стыдно приводить родителя. Если финальные работы
 * уйдут в приёмку — их никто, кроме куратора, не увидит, и пропадёт
 * половина смысла: показать своё и посмотреть чужое.
 *
 * ⚠️ ЭТО ГРУППА, А НЕ КОММЕНТАРИИ ПОД ПОСТОМ. И это вторая попытка.
 *
 * Сначала приёмкой был пост в отдельном канале (`t.me/dz_suda/3`),
 * и уткнулись мы вот во что: **чтобы написать комментарий под постом
 * канала, человек должен сперва вступить в канал И в его чат обсуждений.**
 * Для ребёнка, который зашёл в игру пять минут назад, это три лишних
 * шага до того, как он сможет показать хоть что-то. Владелец 28.07:
 * «очень много еботни».
 *
 * В обычной группе таких порогов нет: перешёл по приглашению, вступил,
 * пишешь. Одно действие вместо трёх — а нам тут важна каждая ступенька,
 * потому что она стоит между ребёнком и его первой сданной работой.
 *
 * ⚠️ АДРЕС ВЕДЁТ В ТЕМУ «Шаги на проверку», А НЕ В ГРУППУ ЦЕЛИКОМ.
 * У группы включены темы, и приглашение высаживало ребёнка в «General»,
 * где писать не надо. Владелец проверил это с другого человека 28.07
 * и прислал адрес темы.
 *
 * ⚠️⚠️ ЦЕНА ЭТОГО РЕШЕНИЯ, ЗНАТЬ ОБЯЗАТЕЛЬНО. Адрес вида `t.me/c/…` —
 * ВНУТРЕННИЙ. Он открывается только у того, кто УЖЕ состоит в группе;
 * посторонний по нему не попадёт никуда. То есть ребёнок из рекламы,
 * который группу ещё не видел, упрётся в пустоту.
 *
 * Чинится это не в коде, а в Телеграме: дать группе публичное имя
 * (Управление → Тип группы → Публичная, `@что-нибудь`). Тогда адрес темы
 * станет `t.me/имя/2`, и он работает у ЛЮБОГО, включая тех, кто в группе
 * ещё не состоит. Как только имя появится — принести адрес сюда.
 *
 * Ссылка-приглашение, с которой начинали: `t.me/+6PZ9L41yLus2Njli` —
 * она пускает кого угодно, но высаживает в «General».
 */
export const HOMEWORK_GROUP: OutboundLink = {
  id: "homework-group",
  title: "Приёмка домашек",
  subtitle: "Тема «Шаги на проверку»: скриншоты шагов, каждый день",
  url: "https://t.me/c/3777638025/2/3",
  kind: "telegram",
  emoji: "📥",
};

/**
 * Бот-помощник по Blender — отдельно и первым: это польза, а не соцсеть.
 *
 * ⚠️ `?start=app` В КОНЦЕ АДРЕСА — НЕ МУСОР, НЕ УБИРАТЬ.
 *
 * Этот бот живёт отдельным проектом (репозиторий `Bot_Vozhatiy_V2`)
 * и сделан он был для МАРАФОНА «Молот Тора»: у него внутри вся программа
 * марафона, и по умолчанию он разговаривает как тамошний куратор —
 * спрашивает про домашку и про то, как продвигается молот.
 *
 * Ребёнок из приложения ни на каком марафоне не был, и такой встречи
 * не понимает. Метка `app` в ссылке решает это молча: Телеграм передаёт
 * её боту первым же сообщением, и тот сразу понимает, что перед ним
 * ученик из приложения, а не марафонец. Никаких вопросов ребёнку
 * задавать не приходится.
 *
 * Уберёшь метку — дети снова станут «марафонцами». Названия режимов
 * заданы в боте, в `resolve_mode` (`api/index.py`).
 */
export const HELPER_BOT: OutboundLink = {
  id: "helper-bot",
  // У помощника есть ИМЯ — Джейки, и здесь оно названо нарочно. Ребёнок
  // должен идти к кому-то, а не к «боту-помощнику»: в самом Телеграме
  // он тоже представляется Джейки, и эти два места обязаны совпадать.
  title: "Спроси Джейки про Blender",
  subtitle: "Помощник школы: объяснит и покажет, какие кнопки жать",
  url: "https://t.me/VozhatyBot?start=app",
  kind: "telegram",
  emoji: "🤖",
};

// Витрина школы: сюда ходят читать и смотреть. Разговор — в SCHOOL_CHAT выше.
export const SOCIAL_LINKS: OutboundLink[] = [
  {
    id: "telegram",
    title: "Telegram",
    subtitle: "Канал школы: новости и разборы",
    url: "https://t.me/jcenterskids",
    kind: "telegram",
    emoji: "✈️",
  },
  {
    id: "youtube",
    title: "YouTube",
    subtitle: "Уроки по Blender",
    url: "https://www.youtube.com/@JKidsBlender",
    kind: "web",
    emoji: "▶️",
  },
  {
    id: "vk",
    title: "ВКонтакте",
    subtitle: "Мы и там тоже",
    url: "https://vk.ru/jcenterskids",
    kind: "web",
    emoji: "🔵",
  },
];

// Внутри мини-аппа обычные ссылки ведут себя плохо — открываем через Telegram.
export function openOutboundLink(link: OutboundLink) {
  try {
    // @ts-ignore
    const webApp = window.Telegram?.WebApp;

    if (link.kind === "telegram" && webApp?.openTelegramLink) {
      webApp.openTelegramLink(link.url);
      return;
    }

    if (webApp?.openLink) {
      webApp.openLink(link.url);
      return;
    }
  } catch {
    // вне Телеграма или старый клиент — открываем обычной вкладкой
  }

  window.open(link.url, "_blank", "noopener,noreferrer");
}
