/**
 * ТЕКСТ ПРИВЕТСТВИЯ — первое, что видит ребёнок. Правит владелец.
 *
 * ══ ЗАЧЕМ ЭТОТ ЭКРАН ВООБЩЕ ПОЯВИЛСЯ ══
 *
 * Приложение дали подростку, который ничего не знал про школу — ни чата,
 * ни ссылок, «просто друг скинул приложуху». Он сказал ровно то, чего
 * не хватало: «не было никакого приветствия, вводной части — для чего
 * создан бот, что это вообще за питомец, что я тут делаю. Меня сразу
 * бросают в среду, где я должен что-то делать и разбираться».
 * И отдельным вопросом: **«для чего мне вообще нужно улучшать
 * этого питомца?»**
 *
 * Обучающий тур отвечал на вопрос ГДЕ («вот комната, вот кнопки»),
 * но не отвечал на вопрос ЗАЧЕМ. Этот экран отвечает на ЗАЧЕМ и стоит
 * ПЕРЕД туром.
 *
 * ══ ПРАВИЛА ДЛЯ ЭТИХ ТРЁХ СТРОК ══
 *
 * 1. Три строки, не больше. Простыню на входе никто не читает.
 * 2. Говорим РЕБЁНКУ и о том, что он получит. Никакой «перспективы»,
 *    «профессии» и «портфолио» — это язык оффера для родителя.
 * 3. Ничего не обещаем сверх правды. Уроков внутри НЕТ, и строки
 *    не должны звучать как «мы тебя научим»: ребёнок придёт
 *    и не найдёт обещанного.
 * 4. Главная мысль, ради которой экран и написан: **призрак растёт
 *    от сделанной работы, а не от времени в приложении.** Это ответ
 *    на «зачем мне его улучшать» и заодно честное описание игры.
 */

export const WELCOME_STORAGE_KEY = "bb_welcome_v1";

export const WELCOME_TITLE = "Привет. Это твой призрак.";

/**
 * Три строки. Порядок важен: сначала кто он, потом что делать,
 * потом что ты с этого получишь.
 */
export const WELCOME_LINES: { emoji: string; text: string }[] = [
  {
    emoji: "👻",
    text:
      "Он растёт не от того, сколько ты тут сидишь, а от того, " +
      "что ты собрал в Blender. Сделал — подрос.",
  },
  {
    emoji: "🎯",
    text:
      "Каждый день тут ждёт задание. Придумывать, чем заняться, " +
      "не надо — уже придумано.",
  },
  {
    emoji: "🏆",
    // «Проект», а не «вещь» — поправка владельца 27.07. За неделю выходит
    // не предмет, а работа целиком: модель, материалы, свет и рендер.
    // Слово «вещь» обесценивает то, что ребёнок на самом деле сделал.
    text:
      "За неделю выходит целый проект: меч, геймпад, робот. " +
      "В конце — твой рендер, который можно показать.",
  },
];

/** Надпись на кнопке. Дальше сразу начинается обучающий тур. */
export const WELCOME_BUTTON = "Погнали";

/**
 * Приписка под кнопкой. Отвечает на «я тут один?» — тот же страх,
 * что и у тестировщика: он не знал ни про чат, ни про других ребят.
 */
export const WELCOME_FOOTNOTE = "Застрял — рядом чат, там свои. Никто не делает это в одиночку.";
